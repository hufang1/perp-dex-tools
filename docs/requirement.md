现在我阐述一下我的诉求，现在程序开仓和平仓都是市价，extended市价收0.025%手续费，lig不手。两次就是0.05%的价差。之前给过你每天的价差分布图。你当时总结的是0.5%的手续费一天只能开10多单，将手续费降低到0.025%或者更低可以开更多的单。

所以我现在的核心诉求就是降低手续费。-> 转变成将extended改为marker的诉求，marker是0手续费的。

我对统计的实时价差进行了分析，发现以下数据

平均价差：~8.6 bps (0.0086%)

最低价差 (Min)：3.94 bps (0.0039%)

最高价差 (Max)：12.49 bps (0.0125%)

然后我想修改一下现有的价差套利程序中的以下逻辑：

1. 不走阶梯开仓的价差的模式，固定一个开仓价差（走配置项，暂定为0.09%），超过就开仓，没超过就不开仓。该值不会变化。 固定一个平仓价差(暂定为0.06%)。平均开仓价差 - 手续费 大于它就可以平仓了。

2. 现在的extended开仓和平仓都走的是带滑点的taker模式。我希望extended开仓和平仓都走marker。这样可以手续费近乎为0.00%。 在原先的基础上可以进行更大的交易量的交易。（在extended等待成交的过程中，如果实时价差不满足对应的开仓价差。取消挂单等待下一次机会）

extended的挂单逻辑可以参考 @hedge/hedge_mode_ext.py中的部分代码。

然后基于spread项目的现有流程结构进行代码分析，考虑是否要调整整体的流程，是否要调整流程状态（HOLDING等）。